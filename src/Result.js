import {questions} from './questionList';

const calculateResult = (answers, scoringSystem) => {
    const resultScores = {};

    answers.forEach((answer) => {
        const { question, choice } = answer;

    })
}